name: Build Subtitle AI Master EXE
on: [push]

jobs:
  build:
    runs-on: windows-latest
    steps:
    - name: Checkout Code
      uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'

    - name: Install Latest Dependencies
      run: |
        pip install --upgrade google-generativeai pyinstaller

    - name: Build Windows EXE
      run: |
        pyinstaller --onefile --noconsole app.py

    - name: Upload Artifact
      uses: actions/upload-artifact@v4
      with:
        name: Subtitle_AI_Master_Software_v3
        path: dist/app.exe
